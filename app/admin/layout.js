export const metadata = {
  title: "Bangers CMS — Content desk",
  description: "Secure content management for the Bangers inspiration board.",
  robots: { index: false, follow: false },
};

export default function AdminLayout({ children }) {
  return children;
}
